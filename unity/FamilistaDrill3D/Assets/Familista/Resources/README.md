# Resources (runtime-loaded)

**Familista → Setup All** writes here:
- FamilistaPlayer.prefab (the player, from your imported character)
- KitBlue.mat / KitRed.mat / KitGK.mat / PitchMat.mat

DrillEngine loads these by name at runtime; if absent it falls back to flat colours.
